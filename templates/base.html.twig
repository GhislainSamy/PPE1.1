<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}<link rel="stylesheet" href="{{ asset('build/app.css') }}">{% endblock %}
    </head>
    <body>
       
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      

 <p><img src="{{ asset('build/images/logo_urc.png') }}"/> URC DRAVEIL    </p>
  <div class="collapse navbar-collapse" id="navbarTogglerDemo03">
    <ul class="navbar-nav mr-auto mt-3 mt-lg-1">
      <li class="nav-item active">
        <a class="nav-link" id= "acc" href="{{path('accueil')}}"> Accueil <span class="sr-only">(current)</span></a>
      </li>
       {% if not app.user %}
       <li class="nav-item active">
        <a class="nav-link" id= "acc" href="{{path('accueil')}}"> Évènement <span class="sr-only">(current)</span></a>
      </li>
            {% endif %}
        {% if is_granted('ROLE_USER') %}
        


 <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Profil Sportif
      </a>
      <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        <a class="dropdown-item" href="{{path('register')}}">Inscription de sportif</a>
        <a class="dropdown-item" href="{{path('useradherent')}}">Liste de mes Profils sportif</a>
        
         {#  <li class="nav-item active">
        <a class="nav-link" id= "acc" href="{{path('accueil')}}">   Évènement <span class="sr-only">(current)</span></a>
      </li> #}
       <li class="nav-item active">
        <a class="nav-link" id= "acc" href="{{path('evenement_indexadh')}}"> Voir Évènement <span class="sr-only">(current)</span></a>

          {% endif %}
        {% if is_granted('ROLE_ADMINISTRATEUR') %}

        <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Adherents
      </a>
      <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        <a class="dropdown-item" href="{{path('adherents')}}">Afficher tout les adherents</a>
        <a class="dropdown-item" href="{{path('accueil')}}">Confirmation Dossier / Payement</a>
        
      </div>
      



 <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Utilisateur
      </a>
      <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        <a class="dropdown-item" href="{{path('user_index')}}">Afficher tout les Utilisateur</a>
        <a class="dropdown-item" href="{{path('user_new')}}"> Ajout User</a>
        
      </div>

    </li>
    

<li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Gestion Évènement
      </a>
      <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
        <a class="dropdown-item" href="{{path('evenement_index')}}">Afficher tout les Évènements</a>
        <a class="dropdown-item" href="{{path('participer_index')}}"> Afficher tout les Participants</a>
        
      </div>

    </li>


      {% endif %}
    </ul>

 {% if not app.user %}
    <a id="conn" class="nav-link" href="{{path('app_login')}}">Se connecter</a>
    {% else %}

  <a id="deconn" class="nav-link" href="{{path('app_logout')}}">Se déconnecter</a> 
{% endif %}
      {#  <a id="conn" class="nav-link" href="{{path('connecter')}}">Se connecter</a> #}
      {#  <a id="deconn" class="nav-link" href="{{path('deconnecter')}}">Se déconnecter</a> #}
  </div>
</nav>
<br>
        {% block body %}{% endblock %}
        {% block javascripts %}
        <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
        <script src="{{asset('build/app.js') }}"></script>{% endblock %}
    </body>
</html>
